@model EFGetStarted.AspNetCore.NewDbPost.Models.Company

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

<h4>Company</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Type" class="control-label"></label>
                @*@Html.EnumDropDownListFor(model => model.TypeCompanies, // Model field that will be used to store user selection
                             "- Please select your industry -", // Text for the first 'default' option
                             new { @class = "form-control" })  @* A class name to assign to "select" tag *@

                            @Html.DropDownList("Type", ViewBag.MiListadoEnum as IEnumerable<SelectListItem>, new { @class = "form-control" })
                            @*other way*@
                            @* <select asp-for="Type" class="form-control" asp-items="ViewBag.MiListadoEnum"></select>*@
                            <span asp-validation-for="Type" class="text-danger"></span>
                        </div>
            <div class="form-group">
                <label asp-for="SSNCompany" class="control-label"></label>
                <input asp-for="SSNCompany" class="form-control" />
                <span asp-validation-for="SSNCompany" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
